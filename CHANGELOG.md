# Changelog

本專案的所有重要變更都記錄在此文件。

## [1.3.0] - 2026-08-01

介面全面改版：建立設計系統，並支援跟隨系統的亮／暗色模式。

### Added
- 新增設計系統 `ui/theme.py`：色彩、間距、字級、圓角統一由 token 管理，色階以 App Icon 的青綠色（OKLCH hue 218）推導
- 支援深色／淺色模式，跟隨 macOS 系統設定即時切換
- 空狀態改為真正的拖放熱區，拖曳檔案經過時會即時高亮回饋（先前拖曳完全沒有視覺回應）
- 按鈕、輸入框、下拉選單、清單項目補齊 hover／pressed／focus／disabled 狀態
- 「刪除選取」按鈕在未選取任何項目時自動停用

### Changed
- 所有文字與背景的對比度經計算驗證達 WCAG AA（≥ 4.5:1），亮暗兩種模式皆適用
- 統一主要按鈕樣式（先前預覽頁與裁切頁的主按鈕顏色、圓角、尺寸各不相同）
- 空狀態標題由 64px 縮至 28px，並補上支援格式說明
- 介面文案改以中文為主，移除按鈕上的英文括號註記
- 尺寸檢查結果改放獨立一行並使用語意色（正常為綠、異常為紅）
- 裁切編輯器標題改為顯示範例檔名與待處理張數
- 打包設定補上版本資訊、Bundle Identifier 與深色模式支援宣告；版號改為自 `APP_VERSION` 自動帶入

### Fixed
- 修復視窗背景透明、可看見桌面與其他視窗內容的問題
- 修復裁切編輯器首次載入時影像未填滿畫布的問題
- 修復異常尺寸圖片的紅色警示被「已選取」樣式覆蓋、導致警告訊號消失的問題（改為在縮圖上繪製紅框並加註 ⚠）
- 修復縮圖檔名在部分尺寸下被截斷的問題

## [1.2.0] - 2026-07-11

> 自本版本起，AI 協作角色由 Antigravity 移交給 Claude Code。

### Fixed
- 修復「格式化（連續編號）」的起始數字設定被忽略的問題（先前不論設定多少，實際輸出永遠從 1 開始編號）
- 修復進度視窗「取消」按鈕無效的問題：現在按下取消會真正停止批次處理（正在處理中的那一張會完成後才停止，避免留下半成品檔案）
- 修復覆蓋模式下「丟垃圾桶失敗仍會永久覆蓋原檔」的資料遺失風險：現在丟垃圾桶失敗會中止處理並保護原圖
- 修復異常尺寸圖片「預設選取」未生效的問題
- 修復「自訂名稱」留空時，實際輸出與預覽不一致的問題
- 修復處理發生錯誤後背景執行緒未正確清理的問題

### Added
- 另存新檔遇到同名檔案時，自動仿 Finder 在檔名後加上 ` (1)`、` (2)`…，不再靜默覆蓋既有檔案（PDF 輸出同樣適用）
- 新增 MIT LICENSE 檔案

### Changed
- 打包設定檔 `ImageCropper.spec` 更名為 `ScrnshotMate.spec`，內部中間產物名稱一併統一（歷史遺留清理）
- 打包目標架構 `target_arch` 由 `universal2` 修正為 `arm64`，與實際依賴（Pillow 為 arm64-only）一致，讓打包能正常完成（原設定實際上無法成功打包）
- 文件結構調整以符合開源慣例：`readme.md` → `README.md`、需求規格移至 `docs/develop.md`、更新日誌獨立為本文件
- 版號改為單一來源管理（`utils/helpers.py` 的 `APP_VERSION`）
- 移除無法觸及的死程式碼（PDF + 覆蓋模式分支）

## [1.1.0] - 2026-04-27
- 新增專屬 App Icon，並更名為 ScrnshotMate
- 優化開始畫面：動態調整 Icon 大小、修復深色模式下的文字顏色顯示

## [1.0.0] - 2026-04-26
- 增加首頁歡迎提示文字
- 裁切介面縮放功能改為拉桿（0.5x ～ 2.0x）
- 應用程式正式發布 1.0 版本

## [0.5.0] - 2026-04-26
- 裁切介面新增圖片縮放功能
- 增加裁切框邊緣粗度（×5）改善操作體驗
- 新增命名規則的即時預覽功能
- 修復 PNG（RGBA）轉存 JPEG 時發生的錯誤

## [0.1.3] - 2026-04-26
- 完成核心開發，使用 PyInstaller 打包為 macOS 獨立應用程式（.app）

## [0.1.2] - 2026-04-26
- 開發框架從 Electron 改為 Python（PySide6）

## [0.1.1] - 2026-04-26
- 確認使用 Electron 開發框架
- 確定只允許單一尺寸裁切、存檔格式為 JPG/PNG/WEBP/PDF、原檔刪除邏輯
- 修正裁切框拖動邊條 UI 細節

## [0.1.0] - 2026-04-26
- 建立初步需求文件
